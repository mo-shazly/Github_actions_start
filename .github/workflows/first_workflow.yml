name: my first workflow | shell script

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
           

            - name: list files
              run:  ls -ltra

            - name: run shell script
              run:
                |
                chmod +x script.sh
                ./script.sh

