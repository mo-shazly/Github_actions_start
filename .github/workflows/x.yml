name: x workflow
on: 
    push:
        branches:
            - main

jobs:
   docker:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout
          uses: actions/checkout@v6.0.2

        - name: echo name
          run: |
           chmod +x script.sh
           ./script.sh

        - uses: actions/upload-artifact@v7
          with:
            name: dragon.txt
            path: dragon.txt

    

        


