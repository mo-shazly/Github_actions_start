name: my sec workflow | shell script

on: 
   push:

   pull_request:
   workflow_dispatch:

jobs:
    hamada:
        runs-on: ubuntu-latest
        steps:

            - name: Checkout
              uses: actions/checkout@v6.0.2
        
            - name: list files
              run:  ls 

            - name: run shell script
              run:
                |
                chmod +x script10.sh
                 ./script10.sh

      


       

