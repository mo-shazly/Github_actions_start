name: C3 COWSAY

on:
 push:
  branches: main

 workflow_dispatch:

jobs:
 ascii_job_c3:
  runs-on: ubuntu-latest
  steps:

  - name: Checkout
    uses: actions/checkout@v7.0.1

  - name: run script
    run: 
     |
     chmod +x script.sh
     ./script.sh


 ascii_job_sec:
  runs-on: ubuntu-latest
  needs: ascii_job_c3
  steps:

  - name: Checkout
    uses: actions/checkout@v7.0.1

  - name: run script
    run: 
     |
     chmod +x script.sh
     ./script.sh